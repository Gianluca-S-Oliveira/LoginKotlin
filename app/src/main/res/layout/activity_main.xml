<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
android:drawableRight="@drawable/folha"
        android:layout_marginTop="400px"
        android:text=""></TextView>

    <EditText
        android:id="@+id/edt_nomee"
        android:layout_width="800px"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="100px"
        android:gravity="top"

android:background="@drawable/inputs"  android:paddingHorizontal="30dp"
        android:paddingVertical="7dp"

        android:drawableRight="@drawable/usuario"
        android:hint="Digite seu nome"
        />

    <EditText
        android:id="@+id/senhaEditText"
        android:layout_width="800px"
        android:layout_height="wrap_content"
        android:layout_gravity="center"

        android:background="@drawable/inputs"
        android:paddingHorizontal="30dp"
        android:paddingVertical="7dp"

        android:drawableRight="@drawable/baseline_password_24"
        android:layout_marginTop="100px"
        android:gravity="top"
        android:hint="Digite sua senha"
        android:inputType="textPassword" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/login"
        android:layout_width="800px"
        android:layout_height="wrap_content"
        android:layout_marginStart="50dp"
android:background="@drawable/background_button"
        android:layout_marginTop="100px"
        android:layout_marginEnd="16dp"
        android:drawableRight="@drawable/baseline_login_24"
        android:paddingHorizontal="30dp"
        android:textSize="20dp"
        android:textColor="@color/white"
        android:text="LOGIN" />

</LinearLayout>